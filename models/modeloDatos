const mongoose = require('mongoose');
const moduleSchema = new mongoose.Schema({

    datos:{
        type: Array
    },

    name:{
        type: String,
        required: true
    }

});

module.exports = mongoose.model('datos', moduleSchema);